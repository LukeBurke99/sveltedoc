enable-pre-post-scripts = true
engine-strict=true
package-manager=pnpm@10.14.0
strict-peer-dependencies=true

# Flatten node_modules structure for vsce compatibility
# Required because vsce's internal npm validation doesn't understand pnpm's symlink structure
shamefully-hoist=true
node-linker=hoisted